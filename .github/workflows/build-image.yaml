name: Build and Push Docker Image
  
on:
  workflow_dispatch:
    
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.0
          
      - name: Extract Docker image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/airflow
          tags: |
            type=sha
      
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
            
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
          
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: true
          sbom: true
            
      - name: Upload build artifacts
        if: ${{ success() }}
        uses: actions/upload-artifact@v5
        with:
          name: provenance-metadata
          path: |
            .buildx/buildkit/**/*.intoto.json
            .buildx/buildkit/**/*.sbom.*

      - name: Trigger Airflow deployment update
        uses: benc-uk/workflow-dispatch@v1
        with:
          token: ${{ secrets.ORG_TOKEN }}
          repository: ${{ secrets.AIRFLOW_REPO }}
          workflow: 'deploy-airflow.yaml'
          ref: master
          inputs: |
            tag: ${{ steps.meta.outputs.tags }}
